function upperName(name){
    return name.toUpperCase();
};

const upperNameDois = function(){
    return name.toUpperCase();
};

const upperNameArrow = (name, lastName) =>{
    return name.toUpperCase();
};

const upperNameTres = name => name.toUpperCase();

const countLetters = word => word.length;

console.Log(countLetters("jeff"));